# spring-boot-docker/docker-compose.yml
version: "3.7"
services:
  db:
    image: mysql:5.7
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: spring-teste
      MYSQL_DATABASE: enquete
      MYSQL_USER: root
      MYSQL_PASSWORD: root
#  web-service:
#    build:
#      context: ./
#      dockerfile: Dockerfile
#    ports:
#      - "8080:8080"
#    networks:
#      - spring-boot-mysql-network
#    depends_on:
#      - mysql-service
volumes:
  db_data: